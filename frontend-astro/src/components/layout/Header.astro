---
// src/components/layout/Header.astro
// Puede que no lo necesites si el Sidebar o las páginas manejan el título y acciones
---
<header class="sticky top-0 z-40 h-16 flex-shrink-0 border-b border-neutral-200 dark:border-neutral-700 bg-white/80 dark:bg-neutral-900/80 backdrop-blur-sm flex items-center px-4 md:px-6 lg:px-8">
    {/* Contenido del Header: Quizás título de la página actual, búsqueda, perfil de usuario */}
    <div class="flex-1">
         {/* <h1 class="text-xl font-semibold text-neutral-800 dark:text-neutral-100">{Astro.props.pageTitle || 'MewAI'}</h1> */}
    </div>
    <div>
        {/* Botón de perfil, notificaciones, etc. */}
    </div>
</header>